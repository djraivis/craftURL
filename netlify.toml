[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback for client routes (none today; safe default for Vite)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
